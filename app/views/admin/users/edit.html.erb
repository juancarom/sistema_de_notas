<% content_for :title, "Editar #{@user.full_name}" %>

<div class="mb-6">
  <%= link_to "← #{@user.full_name}", admin_user_path(@user), class: "text-sm text-gray-500 hover:text-gray-700" %>
  <h1 class="text-2xl font-bold text-gray-800 mt-1">Editar usuario</h1>
</div>

<div class="bg-white rounded-xl shadow p-6 max-w-lg">
  <%= render "form", user: @user %>
</div>
