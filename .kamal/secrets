# Secrets for Sistema de Notas — never commit real values here
# Fill in before first deploy.

# Docker Hub password or access token
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Rails master key (from config/master.key)
RAILS_MASTER_KEY=$(cat config/master.key)

# PostgreSQL password for the production database
SISTEMA_NOTAS_DB_PASSWORD=$SISTEMA_NOTAS_DB_PASSWORD
SISTEMA_NOTAS_DB_PASSWORD_AS_POSTGRES_PASSWORD=$SISTEMA_NOTAS_DB_PASSWORD
